import React from 'react';
const style = {
  textAlign: 'center'
} as const;

const Options: React.FC = () => {
  return(
    <div style= {style}>
      options here
    </div>
  );
}

export default Options;
