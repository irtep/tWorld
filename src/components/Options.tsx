import React from 'react';
const style = {
  textAlign: 'center'
} as const;

const Options: React.FC = () => {
  return(
    <div style= {style}>
      <h3>Trident City</h3>
    </div>
  );
}

export default Options;
